@model IEnumerable<CxC.Datos.FCCxCStoreProc>

@{
    ViewBag.Title = "Index";
}

<h2>Lista</h2>

<p>
    @Html.ActionLink("Crear", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Id_SP)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Nom_SP)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Dsc_SP)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Id_SP)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Nom_SP)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Dsc_SP)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new {  id=item.Id_SP  }) |
            @Html.ActionLink("Detalles", "Details", new {id=item.Id_SP   }) |
            @Html.ActionLink("Borrar", "Delete", new { id=item.Id_SP   })
        </td>
    </tr>
}

</table>
